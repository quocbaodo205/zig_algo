  ## Key Differences & Improvement Plan

  ### 1. Precomputed Root Tables

  • Our code: Computes w_len (primitive root powers) every time in NTT loop
  • Rust code: Precomputes tables of roots of unity and their inverses
  • Plan: Add precomputed root tables for modulus 998244353 up to the maximum needed size

  ### 2. SIMD Optimization

  • Our code: Uses scalar MontgomeryModint
  • Rust code: Uses MontgomeryModintx8 with AVX/AVX2 SIMD via #[target_feature]
  • Plan: Re-implement MontgomeryModintx8 using @Vector(8, u64) and use it for Hadamard product
  (the fa[i] *= fb[i] step in convolution)

  ### 3. Naive Convolution for Small Sizes

  • Our code: Always uses NTT convolution regardless of input size
  • Rust code: Uses naive convolution if shorter input ≤ 8 elements
  • Plan: Add check in convolution function to use naive O(n²) for small inputs

  ### 4. Precomputed Inverse Table

  • Our code: User removed it, but we should add it back (linear sieve recursive formula)
  • Rust code: Uses linear-sieve-generated inverse tables
  • Plan: Re-add the precomputed inverse table for 998244353 (size 200001)

  ### 5. Avoid Unnecessary Allocations/Copies

  • Our fps.zig mul function: Makes a copy of a and allocates a new convolution result, then
  copies back
  • Plan: Optimize mul to avoid unnecessary copies where possible

  ### 6. Optimize NTT Bit-Reversal Permutation

  • Our code: Computes bit-reverse for each index with a loop
  • Possible optimization: Precompute bit-reversed indices or use a faster implementation
  • Plan: Look into optimizing the bit-reversal step

  Let's prioritize these improvements in order of impact.
